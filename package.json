{
  "name": "jarvis-auto-functions",
  "version": "8.0.0",
  "description": "Firebase Cloud Functions — Claude API proxy для Jarvis Auto",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": { "node": "18" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "private": true
}
